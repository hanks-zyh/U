<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.hanks.huatian">


    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="23" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 录音权限 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- 在SDCard中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:name="xyz.hanks.huatian.CustomApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="xyz.hanks.huatian.ui.activity.SplashActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="xyz.hanks.huatian.ui.activity.MainActivity" />
        <activity android:name="xyz.hanks.huatian.ui.activity.FilterActivity" />
        <activity android:name="xyz.hanks.huatian.ui.activity.CreateActivityActivity" />
        <activity android:name="xyz.hanks.huatian.ui.activity.TopicActivity" />
        <activity android:name="xyz.hanks.huatian.ui.activity.ChatActivity" />
        <activity android:name="xyz.hanks.huatian.ui.view.SelectPictureActivity" />
        <activity android:name="xyz.hanks.huatian.ui.activity.PhotoGallery" />
    </application>


</manifest>
